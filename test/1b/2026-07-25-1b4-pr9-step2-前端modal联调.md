# 1b.4 PR9 Step 2 测试报告 — 前端 modal 联调 + 弹窗层级

**日期**: 2026-07-25 20:36
**测试范围**: DataPage.jsx + data-page.css 6 项修复
**命令**: `cd fincontrol-frontend && npm run build`
**结果**: ✅ **116 modules transformed, 1.45s, 0 错误**

## Bug 2 验证 — 内联单只确认 modal

### TC-2.1 dropdown 改 + 点 ✓ 弹内联 modal — ✅ 静态通过
- 触发链：`onChange={...e.target.value}` 设 dirty → `onClick={() => setCategoryConfirmIntent({...})}` 弹内联 modal
- modal 渲染：z-index 1300（在所有其他 modal 之上）
- 文案：`您确认修改【{fundName}】为【{newCategory}】吗？` + `原归属：{originalCategory}（AI 解析）`

### TC-2.2 取消 modal → 保持 dirty — ✅ 逻辑正确
- `data-testid="category-confirm-cancel"` 按钮 → `setCategoryConfirmIntent(null)`
- dropdown 仍显示 dirty 值，状态未变
- 用户可继续编辑或点 ✓ 重试

### TC-2.3 确认 modal → 写库 + 各类小计刷新 — ✅ 逻辑正确
- `data-testid="category-confirm-confirm"` 按钮 → `setCategoryConfirmIntent(null)` + `await confirmOverride(fundName, newCategory)`
- confirmOverride 内：写库 + setCategoryOverrides + setCategoryDirty 删除 + **fetchLatest(1)** + **bumpRefresh()**
- 前端 useMemo derivedCategories 依赖 `[parsedAsset, categoryOverrides, categoryDirty]` → 立即重算
- "各类小计" 表格立即按新 effective category 重新分桶（Bug 6 主路径修复）

## Bug 3 + 5 验证 — 弹窗层级

### TC-3.1 脏 modal 在预览之上 — ✅ z-index 分级
- 预览 modal: `.modal-backdrop.show-confirm-modal` → z-index 1000
- 脏 modal: `.modal-backdrop.show-dirty-modal` → z-index 1100
- 脏 modal 弹在预览之上 ✓

### TC-3.2 批量 modal 在脏 modal 之上 — ✅
- 批量 modal: `.modal-backdrop.show-batch-modal` → z-index 1200
- 在脏 modal 之上 ✓

### TC-3.3 内联 modal 在所有 modal 之上 — ✅
- 内联 modal: `.modal-backdrop.category-confirm-backdrop` → z-index 1300
- 即使其他 modal 都开着，内联 modal 仍能盖住 ✓

## Bug 4 验证 — 单只确认后首页刷新

### TC-4.1 confirmOverride 触发 fetchLatest — ✅
- `await fetchLatest(1)` 在 `setCategoryOverrides` 之后调用
- 首页 store 最新：latestSnapshot.categories 反映 user_correct
- HomePage useEffect [refreshCounter] 触发重拉

### TC-4.2 confirmOverride 触发 bumpRefresh — ✅
- `useAssetSnapshotStore.getState().bumpRefresh()` 调用
- refreshCounter 递增 → HomePage 监听 → 重拉

## Bug 6 验证 — 各类小计实时刷新

### TC-6.1 改 dropdown 即时刷新 — ✅
- 改 dropdown → setCategoryDirty → React re-render → derivedCategories useMemo 重新计算（依赖触发）
- 表格显示"商品类"金额 = 原本+新改的 fund 金额
- 不依赖 confirmOverride（无需入库）

### TC-6.2 主路径 + fallback — ✅
- 主路径：derivedCategories useMemo 实时计算（前端）
- Fallback：confirmOverride 成功后 fetchLatest 异步刷首页（后端）

## Bug 7 验证 — success banner 宽度

### TC-7.1 banner 宽度限制 30vw — ✅ CSS 验证
```css
.success-banner--top,
.success-banner--second {
  width: fit-content;
  max-width: 30vw;     /* 屏宽 30% */
  min-width: 280px;    /* 最小宽度 */
  left: 50%;           /* 居中 */
  right: auto;
  transform: translateX(-50%);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

### TC-7.2 banner 文案缩短 — ✅ JSX 验证
- 原来: `✓ 首页展示日期 X 确认成功！返回首页即可查看当日基金明细` (挡侧边栏首页项)
- 现在: `✓ 已设为 X` (set) / `✓ X 已入库` (kept) — 短小不挡

### TC-7.3 banner 不挡侧边栏 — ✅
- max-width: 30vw + 居中
- 在 1920x1080 屏幕上：30% = 576px，足够显示短文案
- 不会延伸到侧边栏区域

## npm run build 验证
```
> vite build
✓ 116 modules transformed.
dist/index.html                  0.61 kB
dist/assets/index-DJ82qREE.css   36.59 kB
dist/assets/index-B_-3hpEy.js    272.67 kB
✓ built in 1.45s
```

无错误无警告。

## 结论
Step 2 完成：6 项前端修复全部实现并通过静态验证。
- 后续 Step 3 需要 GUI 环境跑端到端集成测试（本机无浏览器，跑不了）
- 用户需要在本地浏览器验证 TC-1.1（上传 0720 → dropdown 显示"固收类"）和 TC-2.3（改 dropdown → 各类小计立即刷新）