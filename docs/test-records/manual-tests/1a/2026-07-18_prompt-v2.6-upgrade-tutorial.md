# 1a.9 prompt_versions v2.5 → v2.6 升级教程（mysql cli 无 GUI）

**时间**：2026-07-18 23:52 (UTC+8)
**配套 SQL**：`.tmp/prompt-v2.6.sql`
**配套 bat**：`.tmp/run-prompt-v2.6.bat`
**配套决策**：`docs/phase-0/decisions.md §决策 8（1a.9 增补）`
**v2.6 行 ID**：8（v2.5 → v2.6）

---

## 0. 为什么升 v2.6

1a.8.8 v2.5 真实 E2E 暴露 2 个"模型表现"问题（已在 v3.2 验收报告记录为已知债）：

| # | 问题 | 现状 | 1a.9 修法 |
|---|---|---|---|
| 1 | **P2 totalAsset 口径不一致** | v2.5 prompt 让模型在 P2 页输出 visible sum（2987.32），fixture 期望顶部"总资产"全账户（7884.68） | v2.6 prompt：明确"total_asset = 截图顶部'总资产'数字，禁止用当前页 visible sum 代替" |
| 2 | **OCR 诺安误读为华安** | 模型把"诺安中证A100"误读为"华安"（P3 可见，但模型 OCR 错） | v2.6 prompt 不直接修复（OCR 是 vision 模型能力，靠重跑 + 真实 API 验证）；后端按真实 E2E 复现率判断 |

**v2.5 → v2.6 关键差异**：

```diff
+ 【1a.9 关键 - 总资产字段读取规则】
+ total_asset = 截图顶部"总资产"数字（无论第几页都应一致；通常显示"总资产 ¥xxxx.xx"在页面最上方）
+   - 顶部"总资产"清晰可见时，**必须**使用顶部数字（例如 7884.68）
+   - 顶部不可见 / 被遮挡 / 在加载中 → 输出 null（不要凭空估算，也不要把当前页基金加总作为 total_asset）
+   - **禁止**：将当前页可见基金的 amount 加总作为 total_asset（页面只是持仓列表，不等于总资产）
+   - 余额类（余额宝等）通常已含在顶部总资产中，按图所见如实输出
```

**沿用 v2.5 规则**（不变）：
- 七大类 canonical 名（货币类/固收类/商品类/A股权益类/海外权益类/港股大中华类/余额类）
- 余额类 holding_profit=null（Alipay 不显示）
- holding_profit / cumulative_profit 双字段
- categories[].funds[] 嵌套结构（禁 holdings + category_summary）

---

## 1. SQL 内容摘要

完整内容见 `.tmp/prompt-v2.6.sql`。核心 INSERT：

```sql
INSERT INTO prompt_versions (prompt_name, version, prompt_content, change_reason) VALUES
('screenshot_parser', 'v2.6', '<完整 prompt 内容>', '1a.9 总资产口径规则：...')
ON DUPLICATE KEY UPDATE
  prompt_content = VALUES(prompt_content),
  change_reason = VALUES(change_reason);
```

`ON DUPLICATE KEY UPDATE` 写法幂等：第一次执行 = INSERT 新行；后续执行 = UPDATE 已存在的 v2.6 行。不会污染 v2.5。

---

## 2. 执行步骤（mysql cli 无 GUI）

### 2.1 直接执行 bat

```bash
cmd /c "C:\Users\lbc19\Desktop\Fincontrol\.tmp\run-prompt-v2.6.bat"
```

输出应分 3 段：
- [1/3] 检查 v2.5/v2.6 当前状态
- [2/3] 执行 v2.6 升级 SQL（含 INSERT ... ON DUPLICATE KEY UPDATE）
- [3/3] 验证 v2.6 落地

### 2.2 或手动执行（debug 备用）

```bash
cd C:\Users\lbc19\Desktop\Fincontrol
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -proot fincontrol --default-character-set=utf8mb4 < .tmp\prompt-v2.6.sql
```

### 2.3 验证 v2.6 落地

```bash
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -proot fincontrol --default-character-set=utf8mb4 -e "SELECT id, version, LENGTH(prompt_content) AS len, change_reason, created_at FROM prompt_versions WHERE prompt_name='screenshot_parser' ORDER BY id DESC LIMIT 3;"
```

期望输出：
```
id  version  len  change_reason
8   v2.6     3859 1a.9 总资产口径规则：...
7   v2.5     3109 1a.8.8 v3.2 修订：...
6   v2.4     2755 1a.8.8 类别归一化 + ...
```

---

## 3. 字符集陷阱（cmd 默认 GBK → 必须 --default-character-set=utf8mb4）

`mysql.exe` 默认用操作系统的字符集（cmd.exe = GBK）。如果不显式指定 `--default-character-set=utf8mb4`，中文 prompt 内容会被错误转码为乱码（已实测）。

**正确**：
```bash
mysql --default-character-set=utf8mb4 ...
```

**错误**（会出现 `ʶ` 等乱码）：
```bash
mysql ...   # 默认 GBK
```

---

## 4. PromptLoaderService 行为验证

`PromptLoaderService` 已实现（1a.7）`SELECT ... ORDER BY id DESC LIMIT 1` + Caffeine cache 策略：

```java
@Cacheable("prompt_versions")
public String get(String promptName) {
    return promptVersionsMapper.selectLatestByName(promptName)
        .getPromptContent();
}
```

升级到 v2.6 后：
- **新进程启动** → MySQL 读到 id=8 v2.6 → 缓存
- **运行中进程** → Caffeine 缓存未失效 → 仍是 v2.5 内容（需重启服务）
- **强制刷新** → 重启 Spring Boot 应用（`mvn spring-boot:run` / IDE 重启）

**真实 E2E 重跑前置条件**：
1. MySQL 已落地 v2.6（步骤 2.3 验证）
2. Spring Boot 已重启（Caffeine cache 失效）
3. 真实 API key 已配 `application-local.yml`
4. 跑 `scripts/1a8/01-real-four-page-e2e.ps1`

---

## 5. 真实 E2E 验证清单（1a.9 commit 2 前置）

升级 v2.6 prompt 后，需用真实 minimax API 重跑四图（`uploads/samples/` 4 张），预期：

| 校验项 | v2.5 现状 | v2.6 期望 |
|---|---|---|
| 4/4 upload + 4/4 parse code=0 | ✅ | ✅ |
| P1 top total = 7884.68 | ✅ | ✅ |
| **P2 top total = 7884.68** | ❌（v2.5 返回 2987.32） | ✅ |
| **P3 top total = 7884.68** | ❓（v2.5 未验证） | ✅ |
| **P4 top total = 7884.68** | ❓ | ✅ |
| dedup unique=19/19 + total=7884.68 | ✅ | ✅ |
| 余额宝 holding=null + cumulative=1.89 | ✅ | ✅ |
| OCR 诺安→华安误读率 | ❓（v2.5 误读） | ❓（v2.6 真实 E2E 复现率） |

---

## 6. 回退策略

如果 v2.6 prompt 触发更多错误，可临时回退到 v2.5：

```sql
-- 方法 1：激活 v2.5（PromptLoader 取 ORDER BY id DESC，单纯 UPDATE id=8 不会让 v2.5 复活）
-- 正确做法：物理删除 v2.6 行，让 v2.5 id=7 重新成为最新
DELETE FROM prompt_versions WHERE id=8;

-- 验证
SELECT id, version FROM prompt_versions WHERE prompt_name='screenshot_parser' ORDER BY id DESC LIMIT 3;
```

**注意**：v2.6 commit 不会自动回退，需要手动执行 DELETE。生产环境慎用。

---

## 7. 引用

- 决策 8 1a.9 增补：`docs/phase-0/decisions.md §决策 8`
- v2.5 教程：`docs/test-records/manual-tests/2026-07-18_prompt-v2.3-upgrade-tutorial.md`（同模式）
- v3.3 验收报告（待 commit 2）：`docs/test-records/manual-tests/2026-07-18_phase1a8-v3_3-real-data-check.md`
- 1a.9 work-plan（待）：`docs/phase-1/work-plans/2026-07-18_phase1a9-work-plan.md`