<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fincontrol.mapper.SettingsMapper">

    <!-- PR3plus 决策 31：settings 表 resultMap -->
    <resultMap id="SettingsMap" type="com.fincontrol.entity.Settings">
        <id column="user_id" property="userId"/>
        <result column="max_snapshot_age_days" property="maxSnapshotAgeDays"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="SettingsMap">
        SELECT user_id, max_snapshot_age_days, created_at, updated_at
        FROM settings
        WHERE user_id = #{userId}
    </select>

    <insert id="upsert" parameterType="com.fincontrol.entity.Settings">
        INSERT INTO settings (user_id, max_snapshot_age_days, created_at, updated_at)
        VALUES (#{userId}, #{maxSnapshotAgeDays}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
            max_snapshot_age_days = VALUES(max_snapshot_age_days),
            updated_at = NOW()
    </insert>

</mapper>
